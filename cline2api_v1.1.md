# Cline2API v1.1 改进记录

版本号：`v1.1.0`。本次为 v1.0 上的增量修复，不改变代理端点与 Free / Pass 分组策略。

## 变更范围

| 模块 | 修改 | 主要文件 |
| --- | --- | --- |
| 后台认证 | 绑定用户的会话、实际改密、角色授权、bcrypt 兼容迁移 | `admin.go`、`admin_auth.go`、`types.go` |
| 管理界面 | 初始改密流程、当前用户改密、普通用户只读界面、中英文提示 | `admin_html.go`、`i18n.go` |
| 数据保存 | 解析失败保护、启动备份、私有原子写入、显式数据目录 | `pool.go`、`storage.go` |
| 代理配置 | 深拷贝快照，避免共享请求头 map 竞态 | `admin.go` |
| Anthropic | 完整工具参数、并行工具索引、失败流状态、保留非流式工具结果 | `proxy.go` |
| 启动 | 移除端口强杀，传递 `-start` 参数 | `main.go`、`proxy.go` |
| 构建发布 | 统一默认版本，PR 检查与发布前测试，Docker 目录挂载 | `VERSION`、`version.go`、`desktop/build.sh`、`.github/workflows/` |

## 后台权限

| 操作 | 管理员 | 普通用户 |
| --- | --- | --- |
| 账号脱敏列表、统计、模型和请求日志 | 允许 | 允许 |
| 修改本人密码 | 允许，需要当前密码 | 允许，需要当前密码 |
| 导出账号凭据、管理 Key、修改设置 | 允许 | 禁止 |
| 新建/删除用户、重置他人密码 | 允许 | 禁止 |

普通用户只读权限仍能查看所有账号邮箱和请求日志元数据，不是按用户隔离的多租户系统。不要向不应看到这些数据的人发放后台账号。

`GET /admin/api/me` 返回当前用户的 ID、用户名、角色、初始改密状态和版本号。
`POST /admin/api/password` 接收 `{currentPassword, password}`；成功后需要重新登录。
旧的「空密码关闭门禁」行为已取消；新密码限制为 8–72 字节。

本机默认凭据保持 `admin / admin`，仅能访问本人信息与改密接口，且不能远程登录。默认密码必须在暴露到网络或配置反向代理之前更换。

## 数据迁移

1. 停止旧程序并备份 `.cline-accounts.json` 及其他配置。
2. 普通 CLI / 桌面升级保留原有路径；可用 `CLINE_PROXY_DATA_DIR` 指定账号、日志和代理配置目录。
3. Docker 将旧配置和 `override.md` 复制到 `./data/`，使用目录挂载；Compose 要求显式设置 `CLINE_ADMIN_PASSWORD`。
4. 启动时从有效配置生成 `.cline-accounts.json.bak` 后再保存迁移结果。解析失败不生成空池、不覆盖损坏文件。
5. 旧单密码字段迁移为 admin 用户后清空。旧用户的非默认密码继续有效；登录时升级哈希，或在后台主动改密。

备份是本次启动迁移前的快照，不是完整历史版本库。其与账号文件均含敏感凭据，文件权限为 `0600`。

## 工具调用行为

Anthropic 文本继续实时输出；工具参数累积到上游结束后校验，再依次发送 `tool_use` 内容块及 `input_json_delta`。这样不会把不完整 JSON 当作 `{}`，也不会让多个工具共用索引。非法参数和异常断流发送错误事件，日志标为失败。

## 版本与发布

默认版本以根目录 `VERSION` 为准；运行时仍支持 `-ldflags "-X main.appVersion=..."`，桌面脚本支持 `VERSION` 环境变量，Docker 支持 `APP_VERSION` 构建参数。

发布说明沿用 `releases/vX.Y.Z.md` 格式。本次准备 `releases/v1.1.0.md`；推送版本标签才会触发 GitHub Release 流程。

## 验证记录

- 验证环境：macOS 27、Apple Silicon，Go 1.27.1。
- 新增 12 个回归测试函数，覆盖认证、权限、存储、配置并发、Anthropic 工具调用和端口冲突。
- `go test -mod=readonly -shuffle=on -count=3 -timeout 120s ./...`：通过。
- `go test -mod=readonly -race -timeout 120s ./...`：通过；另一次开启随机测试顺序的竞态检测也通过。
- `go vet -mod=readonly ./...`、后台 JavaScript 语法、构建脚本语法与 `git diff --check`：通过。
- macOS arm64 CLI 与桌面端构建通过；桌面端 `-selfcheck` 返回健康检查成功。
- 使用独立临时数据目录在浏览器验证登录门禁、初始改密界面、旧会话失效、普通用户只读导航与改密表单。验证数据不包含真实 Cline 凭据。
- 未进行真实模型推理测试；Windows / Linux 桌面运行和 Docker 容器运行未在本机验证，跨平台发布由 CI 检查。

本地产物：`build/bin/cline-proxy-v1.1.0-darwin-arm64` 和 `desktop/build/darwin-arm64/cline-proxy-desktop`。这些二进制属于构建产物，不提交到 Git。

## 后续范围

Responses 多工具流式兼容、请求取消与超时、Token 刷新合并、监听切换回滚、前端拆分及存储性能优化不在本次修复范围内。

## 账号管理体验优化

参考 [codex2api](https://github.com/james-6-23/codex2api/tree/de41a5e3dfe9ff51524af864532c42e37955346f) 的账号列表、卡片、批量操作反馈和详情侧栏交互，基于本项目数据模型独立实现，版本继续为 v1.1.0。

- 状态概览：全部、可参与调度、需要处理、订阅待确认；点击概览即可筛选。账号可参与调度与模型冷却可以同时存在，单个模型受限不表示整个账号不可用。
- 搜索邮箱 / ID，组合筛选订阅和状态，按创建时间、最近使用、累计 Token、请求数或邮箱排序。
- 卡片 / 列表切换，每页 12 / 24 / 48 条；浏览器保存视图偏好。分页在前端处理，后台仍返回完整脱敏账号列表。
- 勾选状态跨页保留，列表刷新只移除已不存在的账号；改变筛选或排序会清空选择，避免隐藏目标误操作。显示跨页选择提示，单次最多 1000 个账号。
- 批量测试、刷新凭据、修改分组、导出所选和删除所选。测试与刷新逐项执行，显示进度和错误结果；停止后续任务不会取消正在执行的上游请求。
- 详情侧栏展示账号 ID、订阅、状态、创建/使用时间、输入/输出/缓存量、模型统计和冷却时间，不展示凭据。
- 批量删除和分组修改先校验全部目标；保存失败回滚内存。新增 `POST /admin/api/accounts/batch-delete`，扩展 `POST /admin/api/accounts/export` 支持 `{accountIds: [...]}`。原 GET 全量导出保持兼容。
- 普通用户仅能查看列表和详情；批量操作、凭据导出仍由后端管理员权限保护。
- 账号页资源拆到 `web/accounts.html`、`accounts.css`、`accounts_model.js`、`accounts.js`，通过 `go:embed` 注入单文件二进制，无需安装前端构建工具。

已接入官方额度和费用查询：管理员可查看个人余额、最近 20 条账单实际扣费，以及有效 Pass 订阅的三窗口已用百分比/重置时间和订阅标价。真实 Free 账号的余额与账单读取已通过；该账号无 Pass，三窗口成功响应通过模拟上游与浏览器交互验证。免费模型剩余百分比仍未确认，健康分应由本地观测定义。详见 [数据来源核查](docs/cline-data-source-verification.md)。

验证：新增 4 个 Go 回归测试和 4 个 Node 数据逻辑测试，覆盖选择范围、权限、保存回滚、筛选、分页与选择状态。使用 26 个独立演示账号执行 DOM 交互测试，覆盖搜索转义、跨页选择、卡片/表格、详情、逐项失败、停止任务和中英文切换。浏览器自动化因连接与系统权限未能完成，本轮不声称完成真实浏览器视觉验证。

## 官方额度和费用接入

- 新增管理员专用 `GET /admin/api/accounts/billing?accountId=...`，可加 `refresh=1` 手动同步。使用上游用户身份，不使用本地 accountId 查询账单。
- 卡片、列表和详情展示官方数据，按个人余额、最近记录实际扣费和订阅标价分别显示；不会把套餐余额、参考成本和实际扣款混算。
- Pass 额度保留已用百分比原值；进度条宽度最高 100%，超过部分仍在数字中显示。未知、未订阅和真实零用量分别展示。
- 同账号并发查询复用结果；上游查询设置超时，失败保留上次数据及其时间。账单只取最近 20 条，明确覆盖范围；普通用户看不到财务数据。
- 凭据刷新与代理共享账号互斥，额度查询不会重新激活冷却账号或改变订阅分组。刷新后的凭据原子保存。
- 新增 9 个 Go 测试、1 个额度数据测试和 3 个金额/额度渲染测试。通过真实浏览器的隔离演示页验证 Pass、Free、失败状态和列表/详情切换；未把演示账号写入真实账号池。
